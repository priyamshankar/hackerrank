#include <iostream>
using namespace std;
void funct(int n)
{
    int a, i, l;
    
    for (i = 0; i < n; i++)
    {
        //if (arr[i] < a)
        {
            l = n;//arr[i];
        }
        cout<<l;
    }
    return funct(n);
}
int main(){

    funct(5);

    return 0;
}